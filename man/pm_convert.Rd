% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pm_convert.R
\name{pm_convert}
\alias{pm_convert}
\title{Convert MBTiles to PMTiles}
\usage{
pm_convert(
  input,
  output,
  force = FALSE,
  no_deduplication = FALSE,
  tmpdir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{input}{Path to input MBTiles file.}

\item{output}{Path for output PMTiles file.}

\item{force}{Logical. If `TRUE`, removes existing output file if present.
Default is `FALSE`.}

\item{no_deduplication}{Logical. If `TRUE`, skips tile deduplication to
speed up conversion. Use this if you know the input has only unique tiles.
Default is `FALSE`.}

\item{tmpdir}{Optional path to a folder for temporary files during conversion.
If not specified, uses the system temporary directory.}

\item{verbose}{Logical. If `TRUE`, prints progress information. Default is `TRUE`.}
}
\value{
Invisibly returns the path to the output archive.
}
\description{
Convert an MBTiles database to PMTiles format. The conversion process
automatically deduplicates tiles unless disabled.
}
\examples{
\dontrun{
# Convert MBTiles to PMTiles
pm_convert("input.mbtiles", "output.pmtiles")

# Convert without deduplication (faster)
pm_convert("input.mbtiles", "output.pmtiles", no_deduplication = TRUE)

# Force overwrite existing file
pm_convert("input.mbtiles", "output.pmtiles", force = TRUE)
}

}
