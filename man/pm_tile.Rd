% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pm_tile.R
\name{pm_tile}
\alias{pm_tile}
\title{Extract a single tile from a PMTiles archive}
\usage{
pm_tile(input, z, x, y, output = NULL, bucket = NULL)
}
\arguments{
\item{input}{Path to a local PMTiles file or URL to a remote archive.}

\item{z}{Integer zoom level.}

\item{x}{Integer tile column.}

\item{y}{Integer tile row.}

\item{output}{Path where the tile should be saved. If `NULL` (default),
returns the raw tile data as a raw vector.}

\item{bucket}{Optional remote bucket specification for cloud storage.}
}
\value{
If `output` is specified, writes tile to file and returns the output path invisibly.
If `output` is `NULL`, returns the tile data as a raw vector.
}
\description{
Fetch one tile from a local or remote PMTiles archive and save it to a file.
}
\examples{
\dontrun{
# Get tile data
tile_data <- pm_tile("archive.pmtiles", z = 0, x = 0, y = 0)

# Save tile to file
pm_tile("archive.pmtiles", z = 5, x = 10, y = 12, output = "tile.mvt")
}

}
